{
    "scripts": {
        "dev": "concurrently  -c \"auto\" \"npm:dev:*\"",
        "dev:tailwind": "npx tailwindcss -i ./app.css -o ./assets/app.css --watch",
        "dev:shopify": "shopify theme dev"
    },
    "devDependencies": {
        "concurrently": "^8.2.2",
        "tailwindcss": "^3.4.4"
    }
}
